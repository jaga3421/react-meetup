/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['upload.wikimedia.org','cdn.icon-icons.com','qph.cf2.quoracdn.net'],
      },
}

module.exports = nextConfig
